----------------------------------------------------
v-model     - атрибут для двухстороннего связывания реактивной переменной (phone, password etc) и элемента, с помощью которого мы взаимодействуем с пользователем

Элементы, которые служат для того, что бы мы взаимодействовали с пользователем: input(все)

реактивные переменные обьявляются в data(){return{}}

короче просто связывает инпут с реактивной переменной

----------------------------------------------------

----------------------------------------------------
v-on  @     -
----------------------------------------------------

----------------------------------------------------
v-bind :    - нужно для связки пропса (в дочернем компоненте) и свойства из data() (в родительском компоненте), еще нужен для того, чтобы значение внутри имело доступ к скрипту
----------------------------------------------------

----------------------------------------------------
v-show      - display: none делает
----------------------------------------------------

----------------------------------------------------
v-for"element in nameArray" :key="element"     

<li v-for="element in test" :key="element"> 
    {{ element }}                               - пример использования
</li>
----------------------------------------------------

----------------------------------------------------
:class="{ 'active': element.isActive }"         -
----------------------------------------------------


----------------------------------------------------
@submit.prevent="" - автоматом даст prevent default (модификатор)
----------------------------------------------------



----------------------------------------------------
HOOKS
----------------------------------------------------
beforeCreate() {
		// Перед тем как компонент создается
		// Ни к this, ни к HTML элементам доступа нет
		// Потому что их не существует
	},

	created() {
		// Компонент инициализировался
		// Все реактивные свойства доступны
		// Есть доступ к this
	},

    beforeMount(){
        //компонент смонтирован, но HTML элементы еще не нарисованы
    },

    mounted(){
        //компонент смонтирован и HTML элементы нарисованы
    },

	beforeUpdate() {
		// Если компонент принимает props
		// И в случае, если props меняется
		// То компонент обновляется
		// Данный хук вызывается перед обновлением
	},

	updated() {
		// Срабатывает после того, как компонент обновился
	},

	beforeDestroy() {
		// Срабатывает перед уничтожением компонента
		// Мы имеем доступ к this
	},

	destroyed() {
		// Срабатывает после уничтожения компонента
		// Ни к this, ни к HTML элементам доступа нет
	}
----------------------------------------------------


----------------------------------------------------

components:{
    LifeCycleHooks: () => import('@/components/LifecycleHooks.vue')             - динамический импорт 
  },


----------------------------------------------------
npm install bootstrap-vue - установить буцтрап для вью
----------------------------------------------------
